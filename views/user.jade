extends layout

block content
	.container
		form.form-signin(action="/user", method="post")
			h2.form-signin-heading Administrator sign in
			input.input-block-level(type="text", name="username", placeholder="Email address")
			input.input-block-level(type="password", name="password", placeholder="Password")
			label.checkbox.
				<div class='col-sm-4'><input type="checkbox" value="remember-me" /> Remember me</div>
			//button.btn.btn-large.btn-primary(type="submit") Sign in